#include <iostream>
#include <string>

bool checkIfPermutated(std::string one, std::string two) {
  bool characterSet[128];
  if(one.size() != two.size()) { return false; }
  int stringSize = one.size();
  for(int i = 0; i < stringSize; i++) {
       
  }
}
